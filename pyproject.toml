[tool.poetry]
name = "bunq_hackathon"
version = "0.1.0"
description = ""
authors = ["Evrim Öztamur <evrim@ccount.nl>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
